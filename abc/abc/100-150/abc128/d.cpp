#include <bits/stdc++.h>
using namespace std;
int main () {
    deque<long> data;
    int N, K;
    cin >> N >> K;
    long V, ans;
    for (int i = 0; i < N;i++){
        cin >> V;
        data.push_back(V);
    }
    
}