#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < n; i++)
#define ll long long
using namespace std;
vector<int> l;
int serch(int a){
    
}
int main()
{
    int n, a, b, c;
    cin >> n >> a >> b >> c;
    l.resize(n);
    rep(i, n)
        cin >>l[i];
    sort(l.begin(), l.end(),greater<int>());
    int ans;
    int tmp;
}
