#include <bits/stdc++.h>
#define rep(i, n) for (long long i = 0; i < (n); i++)
using namespace std;
int main()
{
    int n, k;
    cin >> n >> k;
    long long mod = 1000000007;
    vector<long long> list(n+1);
}